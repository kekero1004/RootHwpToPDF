# 단일 EXE 파일로 빌드하는 방법

## 개요
HwpToPdf 프로젝트를 단일 EXE 파일로 빌드하는 방법을 설명합니다. 단일 EXE 파일은 모든 종속성을 포함하여, 배포와 설치가 간편합니다.

## 선행 조건
- Visual Studio 2019 이상 (Community, Professional 또는 Enterprise 버전)
- .NET SDK 8.0 이상
- 한글 프로그램 설치 및 COM 등록 (check_hwp.bat 스크립트 실행)

## 방법 1: Visual Studio 사용

### 1. 프로젝트 속성 설정
1. Visual Studio에서 HwpToPdf 프로젝트를 엽니다.
2. 솔루션 탐색기에서 HwpToPdf 프로젝트를 마우스 오른쪽 버튼으로 클릭하고 "속성"을 선택합니다.
3. "게시" 탭으로 이동합니다.
4. "게시 프로필" 설정에서 "폴더에 게시"를 선택하고 "완료"를 클릭합니다.
5. "배포 모드" 섹션에서 다음 설정을 적용합니다:
   - 배포 모드: 자체 포함(Self-contained)
   - 단일 파일 생성: 체크
   - 최종 사용자가 압축을 풀 필요 없이 기본 라이브러리 포함: 체크
   - 런타임: win-x64
6. "저장" 버튼을 클릭합니다.

### 2. 발행(Publish) 실행
1. 솔루션 탐색기에서 HwpToPdf 프로젝트를 마우스 오른쪽 버튼으로 클릭합니다.
2. "발행..."을 선택합니다.
3. 게시 설정이 올바른지 확인한 후 "발행" 버튼을 클릭합니다.
4. 지정한 발행 폴더에 HwpToPdf.exe 파일이 생성됩니다.

## 방법 2: 명령줄 사용 (단일 EXE 파일 빌드)
프로젝트 루트 디렉토리에 있는 `single_exe_build.bat` 파일을 실행합니다.

```
.\single_exe_build.bat
```

이 배치 파일은 프로젝트를 Release 모드로 빌드하고, 단일 EXE 파일로 발행합니다.
빌드된 파일은 `bin\SingleExe` 디렉토리에 생성됩니다.

## 주의사항
- COM 참조로 인해 .NET Core 버전의 MSBuild로는 빌드가 실패할 수 있습니다.
- 이 경우 Visual Studio를 통한 발행(Publish)이 가장 안정적인 방법입니다.
- 단일 EXE 파일 크기는 상당히 클 수 있습니다 (~50MB 이상). 이는 모든 종속성이 포함되기 때문입니다.

## 문제 해결
빌드 또는 발행 중 문제가 발생하면:
1. Visual Studio가 최신 버전인지 확인합니다.
2. .NET SDK가 올바르게 설치되어 있는지 확인합니다.
3. 한글 프로그램이 설치되어 있고 COM 객체가 등록되어 있는지 확인합니다 (check_hwp.bat 실행).
4. 발행 설정에서 "트리밍 사용" 옵션을 끕니다 (호환성 문제 해결에 도움이 될 수 있음). 